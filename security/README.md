# Security Platform

The security platform owns SSH, GPG, secrets, hardening, recovery, and permission policy.

Security changes should be explicit, documented, and reversible. Usability matters, but silent risky defaults do not belong in a lifelong workstation.
